services:

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: otkup
      POSTGRES_PASSWORD: otkup
      POSTGRES_DB: otkup
    ports:
      - "5432:5432"
    volumes:
      - otkup_pg:/var/lib/postgresql/data

volumes:
  otkup_pg:
